FROM python:3.12-slim
WORKDIR /app

# leak-agent service — not yet implemented
# LLM orchestration via pydantic-ai
# Called internally by api/, not directly from web/

CMD ["echo", "agent service not yet implemented"]
